spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/XXXXX?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&userSSL=false
    password:
    username:
    hikari:
      max-lifetime: 500000
      minimum-idle: 3
      maximum-pool-size: 10
      connection-test-query: select 1
  redis:
    database: 5
    host: localhost
    port: 6379
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB

  // 阿里云oss配置
aliyun:
  oss:
    access-key:
    secret-key:
    end-point:
    pre-point:
    bucket-name:

server:
  port: 8282